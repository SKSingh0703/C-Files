#include <stdio.h>
int main(){
    int n,sum,r,ld;
    printf("Enter the Number:");
    scanf("%d",&n);
    while(n>0){
        ld=n%10;
        sum=sum+ld;
        r=r*10;
        r=r+ld;
        n=n/10;
    }
    printf("The Sum of the digits is %d and reverse number is %d",sum,r);
}